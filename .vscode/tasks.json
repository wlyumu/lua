{
    "tasks": [
        {
            "type": "shell",
            "label": "g++.exe build active file",
            "command": "F:/MINGW/bin/gcc.exe",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "F:/MINGW/bin/"
            }
        }
    ],
    "version": "2.0.0"
}